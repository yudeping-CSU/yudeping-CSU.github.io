# Deping Yu Academic Homepage

This repository contains a lightweight static academic homepage for Deping Yu, Lecturer at the School of Business, Central South University.

The site is designed for GitHub Pages and uses only plain HTML and CSS. There is no build step, package manager, backend, or JavaScript framework.

## Local Preview

Open `index.html` directly in a browser, or run a small local server from this directory:

```bash
python3 -m http.server 8000
```

Then visit:

```text
http://localhost:8000
```

## Deploy to GitHub Pages

For a GitHub user homepage:

1. Create or rename the GitHub repository to `yudeping-CSU.github.io`.
2. Push these files to the repository's `main` branch.
3. In GitHub, open `Settings -> Pages`.
4. Set the source to deploy from the `main` branch and the repository root.
5. Visit `https://yudeping-CSU.github.io` after GitHub Pages finishes deploying.

The public site URL will be `https://yudeping-CSU.github.io`.

## Content Source

The academic profile, publications, projects, and contact information are based on the official Central South University Business School profile:

<https://bs.csu.edu.cn/info/1793/24263.htm>

The profile photo is loaded from the official Central South University Business School profile page.
